# PostgreSQL Master Host-Based Authentication
# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Database administrative login by Unix domain socket
local   all             postgres                                peer

# "local" is for Unix domain socket connections only
local   all             all                                     peer

# IPv4 local connections:
host    all             all             127.0.0.1/32            scram-sha-256
host    all             all             ::1/128                 scram-sha-256

# Docker network connections
host    all             all             172.20.0.0/16           scram-sha-256

# Application connections
host    spatial_platform    spatial_admin    172.20.0.0/16      scram-sha-256
host    spatial_platform    spatial_reader   172.20.0.0/16      scram-sha-256

# Replication connections
host    replication     replicator      172.20.0.0/16          scram-sha-256

# Allow connections from specific services
host    all             all             postgres-replica/32     scram-sha-256

# Deny all other connections
host    all             all             0.0.0.0/0               reject